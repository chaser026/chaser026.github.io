# 7. 实验

> [!NOTE]
> **本节目标**
> - 掌握学习率对模型训练稳定性与收敛速度的影响，并通过消融实验找到最优值
> - 理解线性缩放原则，学会在不同 Batch Size 下合理匹配学习率
> - 掌握 Transformer 关键组件（RMSNorm、RoPE、Pre/Post-Norm、SwiGLU）的消融实验方法
> - 能够独立设计并运行完整的消融实验流程，并通过 WandB 监控训练曲线

---

## 7.1 学习率消融实验

> [!NOTE]
> **本节目标**
> - 通过扫描不同学习率，观察训练 Loss 曲线，确定最优学习率范围
> - 理解 `min_lr` 与 `max_lr` 的关系及其对余弦退火调度的影响

学习率是控制模型权重更新步长的核心超参数。本实验在固定其他超参数的前提下，扫描一组从保守到激进的学习率，观察训练稳定性与收敛速度。

### 7.1.1 实验配置

| 参数 | 值 | 说明 |
|---|---|---|
| `MAX_ITERS` | 7000 | 总训练步数 |
| `WARMUP_ITERS` | 700 | 预热步数（占 10%） |
| `MAX_NORM` | 1.0 | 梯度裁剪阈值 |
| `BATCH_SIZE` | 32 | 批次大小 |
| `CONTEXT_LEN` | 256 | 上下文长度 |
| `VOCAB_SIZE` | 10000 | 词表大小 |
| `num_layers` | 4 | Transformer 层数 |
| `num_heads` | 16 | 注意力头数 |
| `d_model` | 512 | 模型隐藏维度 |
| `d_ff` | 1344 | FFN 中间维度 |

待测试的学习率列表：

$$
\text{LR\_LIST} = \{10^{-4},\ 3\times10^{-4},\ 6\times10^{-4},\ 10^{-3},\ 3\times10^{-3},\ 6\times10^{-3},\ 10^{-2},\ 3\times10^{-2}\}
$$

每个学习率对应的 `min_lr` 按如下规则自动计算：

$$
\text{min\_lr} = \text{max\_lr} \times 0.1
$$

> [!TIP]
> 使用 `awk` 处理浮点运算，确保 `min_lr` 始终为 `max_lr` 的 10%，避免手动计算误差。

### 7.1.2 实验脚本

```bash
#!/bin/bash

# --- 1. 定义要测试的学习率列表 ---
# 建议覆盖从保守到激进的范围
LR_LIST=(1e-4 3e-4 6e-4 1e-3 3e-3 6e-3 1e-2 3e-2)
# --- 2. 固定参数配置 ---
MAX_ITERS=7000
WARMUP_ITERS=700
MAX_NORM=1.0
BATCH_SIZE=32
CONTEXT_LEN=256
VOCAB_SIZE=10000

# 记录基础路径
OUTPUT_ROOT="model_result/sweep_lr"
WANDB_PROJECT="cs336-pretraining-TinyStories-LR"

# --- 3. 开始循环实验 ---
for LR in "${LR_LIST[@]}"; do
    
    # 自动计算 min_lr = lr * 0.1
    # 使用 awk 处理浮点运算，保证 min_lr 永远是 max_lr 的 10%
    MIN_LR=$(awk "BEGIN {print $LR * 0.1}")
    
    RUN_NAME="lr${LR}_min${MIN_LR}_step${MAX_ITERS}"
    OUT_DIR="${OUTPUT_ROOT}/${RUN_NAME}"
    
    echo "---------------------------------------------------------"
    echo "📊 实验启动: max_lr=$LR, min_lr=$MIN_LR"
    echo "📝 监控名称: $RUN_NAME"
    echo "---------------------------------------------------------"

    uv run python main_train.py \
        --train_data_path data/TinyStoriesV2-GPT4-train.bin \
        --valid_data_path data/TinyStoriesV2-GPT4-valid.bin \
        --run_name "$RUN_NAME" \
        --vocab_size "$VOCAB_SIZE" \
        --num_layers 4 --num_heads 16 --d_model 512 --d_ff 1344 \
        --max_iters "$MAX_ITERS" \
        --batch_size "$BATCH_SIZE" \
        --context_length "$CONTEXT_LEN" \
        --lr "$LR" \
        --min_lr "$MIN_LR" \
        --warmup_iters "$WARMUP_ITERS" \
        --max_norm "$MAX_NORM" \
        --out_dir "$OUT_DIR" \
        --device cuda \
        --wandb_project "$WANDB_PROJECT"

    # 如果 Loss 炸了（NaN），记录并继续跑下一个
    if [ $? -ne 0 ]; then
        echo "❌ 警告: 学习率 $LR 导致训练中断，跳过。"
    fi
done

echo "🎉 所有 5 组学习率消融实验跑完！请打开 WandB 观察曲线。"
```

> [!WARNING]
> 当学习率过大（如 $3\times10^{-2}$）时，Loss 可能出现 NaN 导致训练崩溃。脚本已通过 `$?` 检测退出码并自动跳过，但建议在 WandB 中确认曲线是否正常。

---

## 7.2 Batch Size 消融实验与线性缩放原则

> [!NOTE]
> **本节目标**
> - 理解线性缩放原则的数学原理与适用场景
> - 学会根据训练瓶颈（迭代步数 vs Token 总量）选择合适的 Batch Size
> - 掌握在不同 Batch Size 下自动计算匹配学习率的方法

### 7.2.1 线性缩放原则

线性缩放原则是大模型分布式训练中的一项核心准则：

> 当你将 Batch Size 扩大 $k$ 倍时，应当同步将学习率也扩大 $k$ 倍。

其核心逻辑在于：确保模型在"看过相同数量的数据"后，权重的总更新量级和训练轨迹保持基本一致。由于较大的 Batch Size 意味着梯度估计的噪声更小、置信度更高，模型可以承受更大的步长（学习率）而不会崩溃，从而在固定算力预算下显著提升收敛效率。

数学表达：

$$
\text{lr}_{\text{new}} = \text{lr}_{\text{base}} \times \frac{\text{BS}_{\text{new}}}{\text{BS}_{\text{base}}}
$$

### 7.2.2 如何选择 Batch Size

在工业界，选择参数的逻辑通常基于你的瓶颈在哪里：

- **场景 A：瓶颈是"迭代步数"（Iteration-limited）**
  - 例如作业要求只能跑 7000 步
  - 策略：在显存允许的情况下，使用最大的 Batch Size，并根据线性缩放原则匹配最高的学习率
  - 选择：大 Batch Size（如 64、128、256）

- **场景 B：瓶颈是"Token 总数"（Data-limited）**
  - 例如只有 1 亿个 Token，跑完就没了
  - 策略：小 Batch Size 往往能提供更精细的梯度更新。虽然大 Batch Size 跑得快，但如果两组实验都只看 1 亿个 Token，小 Batch Size 最终的 Loss 往往比大 Batch Size 还要低一点点
  - 选择：小 Batch Size（如 1、8、32）

### 7.2.3 实验配置

| 参数 | 值 | 说明 |
|---|---|---|
| `BASE_BS` | 32 | 基准 Batch Size（对应最优 lr=3e-4） |
| `BASE_LR` | 3e-4 | 基准学习率 |
| `MAX_ITERS` | 7000 | 总迭代步数（保持一致） |
| `WARMUP_ITERS` | 700 | 预热步数 |
| `MAX_NORM` | 1.0 | 梯度裁剪阈值 |

待测试的 Batch Size 列表：

$$
\text{BS\_LIST} = \{1,\ 8,\ 32,\ 64,\ 128,\ 256,\ 512\}
$$

> [!NOTE]
> 脚本中 `BS_LIST` 被设置为 `(1)`，实际运行时需根据显存情况调整。若 512 导致显存溢出（OOM），脚本会自动跳过并继续。

### 7.2.4 实验脚本

```bash
#!/bin/bash

# --- 1. 核心参数设置 ---
# 基于你之前的实验结果
BASE_BS=32              # 之前跑出 3e-4 时的基准 Batch Size
BASE_LR=0.0003          # 也就是 3e-4
MAX_ITERS=7000          # 保持总迭代步数一致
WARMUP_ITERS=700        # 10% 的预热
MAX_NORM=1.0

# --- 2. 待测试的 Batch Size 列表 ---
# 从极小(1, 8)到典型值(64, 128)再到极限值(256, 512)
# 注意：如果 512 导致显存溢出(OOM)，脚本会自动跳过并继续
BS_LIST=(1 8 32 64 128 256 512)
BS_LIST=(1)

# --- 3. 路径配置 ---
OUTPUT_ROOT="model_result/sweep_bs"
WANDB_PROJECT="cs336-pretraining-TinyStories-BS"

# --- 4. 开始循环实验 ---
for BS in "${BS_LIST[@]}"; do
    
    # 【核心数学逻辑】线性缩放学习率
    # 公式: 当前LR = 最佳基准LR * (当前BS / 基准BS)
    LR=$(awk "BEGIN {print $BASE_LR * ($BS / $BASE_BS)}")
    
    # 保持 min_lr 为当前 max_lr 的 10%
    MIN_LR=$(awk "BEGIN {print $LR * 0.1}")
    
    RUN_NAME="bs${BS}_lr${LR}_step${MAX_ITERS}"
    OUT_DIR="${OUTPUT_ROOT}/${RUN_NAME}"
    
    echo "========================================================="
    echo "🚀 启动实验: Batch Size = $BS"
    echo "📈 线性缩放学习率: $LR (Min: $MIN_LR)"
    echo "========================================================="

    uv run python main_train.py \
        --train_data_path data/TinyStoriesV2-GPT4-train.bin \
        --valid_data_path data/TinyStoriesV2-GPT4-valid.bin \
        --run_name "$RUN_NAME" \
        --vocab_size 10000 \
        --num_layers 4 --num_heads 16 --d_model 512 --d_ff 1344 \
        --max_iters "$MAX_ITERS" \
        --batch_size "$BS" \
        --context_length 256 \
        --lr "$LR" \
        --min_lr "$MIN_LR" \
        --warmup_iters "$WARMUP_ITERS" \
        --max_norm "$MAX_NORM" \
        --out_dir "$OUT_DIR" \
        --device cuda \
        --wandb_project "$WANDB_PROJECT"

    # 错误处理：如果显存溢出或其他原因导致崩溃，记录并尝试下一个
    if [ $? -ne 0 ]; then
        echo "⚠️ 警告: Batch Size $BS 运行失败（可能是 OOM），正在尝试下一组..."
        continue
    fi
done

echo "🎉 所有 Batch Size 消融实验已跑完！"
```

---

## 7.3 Baseline 配置

> [!NOTE]
> **本节目标**
> - 建立统一的 Baseline 模型配置，作为后续所有消融实验的对照基准
> - 确认 Baseline 的各项超参数与之前实验中最优值一致

Baseline 采用当前实验中表现最优的配置组合：

| 组件 | 选择 |
|---|---|
| 归一化 | RMSNorm |
| 位置编码 | RoPE |
| 归一化位置 | Pre-norm |
| 激活函数 | SwiGLU |

关键超参数：

| 参数 | 值 |
|---|---|
| `batch_size` | 64 |
| `lr` | 6e-4 |
| `min_lr` | 6e-5 |
| `max_iters` | 7000 |
| `warmup_iters` | 700 |
| `context_length` | 256 |
| `num_layers` | 4 |
| `num_heads` | 16 |
| `d_model` | 512 |
| `d_ff` | 1344 |
| `vocab_size` | 10000 |

### 7.3.1 Baseline 运行命令

```bash
uv run python main_train.py \
    --train_data_path data/TinyStoriesV2-GPT4-train.bin \
    --valid_data_path data/TinyStoriesV2-GPT4-valid.bin \
    --run_name "baseline_bs64_lr6e4" \
    --vocab_size 10000 \
    --num_layers 4 --num_heads 16 --d_model 512 --d_ff 1344 \
    --max_iters 7000 \
    --batch_size 64 \
    --context_length 256 \
    --lr 6e-4 \
    --min_lr 6e-5 \
    --warmup_iters 700 \
    --out_dir model_result/TinyStories_baseline \
    --wandb_project "cs336-pretraining-TinyStories-Ablations" \
    --device cuda
```

---

## 7.4 组件消融实验

> [!NOTE]
> **本节目标**
> - 通过逐一替换或移除关键组件，量化每个组件对模型性能的贡献
> - 学会使用 `--no_rms_norm`、`--norm_mode`、`--no_rope`、`--ffn_type` 等参数控制实验变量

以下所有消融实验均以 7.3 节的 Baseline 配置为基准，仅改变目标组件。所有实验共用相同的 WandB Project：`cs336-pretraining-TinyStories-Ablations`。

### 7.4.1 消融实验总览

| 实验编号 | 变量 | 参数标志 | Run Name |
|---|---|---|---|
| 1 | 移除 RMSNorm | `--no_rms_norm` | `ablation_no_rms_norm` |
| 2 | Pre-norm → Post-norm | `--norm_mode "post"` | `ablation_post_norm` |
| 3 | 移除 RoPE | `--no_rope` | `ablation_no_rope` |
| 4 | SwiGLU → SiLU | `--ffn_type "silu"` | `ablation_silu` |

### 7.4.2 w/ vs w/o RMSNorm

移除 RMSNorm，观察归一化方式对训练稳定性的影响。

```bash
uv run python main_train.py \
    --train_data_path data/TinyStoriesV2-GPT4-train.bin \
    --valid_data_path data/TinyStoriesV2-GPT4-valid.bin \
    --no_rms_norm \
    --run_name "ablation_no_rms_norm" \
    --vocab_size 10000 \
    --num_layers 4 --num_heads 16 --d_model 512 --d_ff 1344 \
    --max_iters 7000 \
    --batch_size 64 \
    --context_length 256 \
    --lr 6e-4 \
    --min_lr 6e-5 \
    --warmup_iters 700 \
    --out_dir model_result/TinyStories_ablation_no_rms_norm \
    --wandb_project "cs336-pretraining-TinyStories-Ablations" \
    --device cuda
```

### 7.4.3 Pre-Norm vs Post-Norm

将归一化位置从 Pre-norm 改为 Post-norm，对比两种架构的训练动态。

```bash
uv run python main_train.py \
    --train_data_path data/TinyStoriesV2-GPT4-train.bin \
    --valid_data_path data/TinyStoriesV2-GPT4-valid.bin \
    --norm_mode "post" \
    --run_name "ablation_post_norm" \
    --vocab_size 10000 \
    --num_layers 4 --num_heads 16 --d_model 512 --d_ff 1344 \
    --max_iters 7000 \
    --batch_size 64 \
    --context_length 256 \
    --lr 6e-4 \
    --min_lr 6e-5 \
    --warmup_iters 700 \
    --out_dir model_result/TinyStories_ablation_post_norm \
    --wandb_project "cs336-pretraining-TinyStories-Ablations" \
    --device cuda
```

> [!TIP]
> Pre-norm（在子层输入前归一化）通常训练更稳定；Post-norm（在子层输出后归一化）在某些设置下可能获得更好的最终性能，但对学习率更敏感。

### 7.4.4 w/ vs w/o RoPE

移除旋转位置编码（RoPE），使用绝对位置编码或无位置编码作为对照。

```bash
uv run python main_train.py \
    --train_data_path data/TinyStoriesV2-GPT4-train.bin \
    --valid_data_path data/TinyStoriesV2-GPT4-valid.bin \
    --no_rope \
    --run_name "ablation_no_rope" \
    --vocab_size 10000 \
    --num_layers 4 --num_heads 16 --d_model 512 --d_ff 1344 \
    --max_iters 7000 \
    --batch_size 64 \
    --context_length 256 \
    --lr 6e-4 \
    --min_lr 6e-5 \
    --warmup_iters 700 \
    --out_dir model_result/TinyStories_ablation_no_rope \
    --wandb_project "cs336-pretraining-TinyStories-Ablations" \
    --device cuda
```

### 7.4.5 SwiGLU vs SiLU

将 FFN 激活函数从 SwiGLU 替换为 SiLU，对比两种门控机制的效果。

```bash
uv run python main_train.py \
    --train_data_path data/TinyStoriesV2-GPT4-train.bin \
    --valid_data_path data/TinyStoriesV2-GPT4-valid.bin \
    --ffn_type "silu" \
    --run_name "ablation_silu" \
    --vocab_size 10000 \
    --num_layers 4 --num_heads 16 --d_model 512 --d_ff 1344 \
    --max_iters 7000 \
    --batch_size 64 \
    --context_length 256 \
    --lr 6e-4 \
    --min_lr 6e-5 \
    --warmup_iters 700 \
    --out_dir model_result/TinyStories_ablation_silu \
    --wandb_project "cs336-pretraining-TinyStories-Ablations" \
    --device cuda
```

> [!NOTE]
> SwiGLU 通过门控机制引入了额外的参数和非线性，通常在相同参数量下优于 SiLU。本实验可验证这一结论在小模型上是否依然成立。\n